# Texture generator utility

Creates a texture map for a minecraft model.

The input texture is projected onto the front of the model, stretching it in the Z direction.

Used in the Koog mod to create spheres and their corresponding textures.
